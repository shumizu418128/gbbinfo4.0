---
import SiteFrame from "~/layouts/SiteFrame.astro";
import { WildcardRegulationContent } from "~/views/2025/WildcardRegulationContent";
import { getCommonYearData } from "~/util/staticPaths";
import { locales } from "@paraglide/runtime.js";

export async function getStaticPaths() {
  const YEAR = 2025;
  const common = await getCommonYearData(YEAR);
  return locales.map((locale) => ({
    params: { lang: locale },
    props: { locale, ...common },
  }));
}

const { locale, yearWithCountry, years, latestYearWithCountry } = Astro.props;
const heroSubtitle = "Wildcard Regulation";
const pageTitle = `GBB ${yearWithCountry.year} ${heroSubtitle}`;
---

<SiteFrame
  locale={locale}
  pageTitle={pageTitle}
  heroSubtitle={heroSubtitle}
  yearWithCountry={yearWithCountry}
  years={years}
  latestYearWithCountry={latestYearWithCountry}
>
  <WildcardRegulationContent locale={locale} year={yearWithCountry.year} />
</SiteFrame>
