---
import SiteFrame from "~/layouts/SiteFrame.astro";
import { WildcardsContent } from "~/views/2024/WildcardsContent";
import { getCommonYearData } from "~/util/staticPaths";
import { locales } from "@paraglide/runtime.js";
import * as m from "@paraglide/messages.js";
import { WILDCARD } from "~/constants/i18nTerms.js";

export async function getStaticPaths() {
  const YEAR = 2024;
  const common = await getCommonYearData(YEAR);
  return locales.map((locale) => ({
    params: { lang: locale },
    props: { locale, ...common },
  }));
}

const { locale, yearWithCountry, years, latestYearWithCountry } = Astro.props;
const subtitle = m.wildcard_list({ Wildcard: WILDCARD });
const title = `GBB ${yearWithCountry.year} ${subtitle}`;
---

<SiteFrame
  locale={locale}
  title={title}
  subtitle={subtitle}
  yearWithCountry={yearWithCountry}
  years={years}
  latestYearWithCountry={latestYearWithCountry}
>
  <WildcardsContent locale={locale} year={yearWithCountry.year} />
</SiteFrame>
