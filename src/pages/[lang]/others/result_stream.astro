---
import SiteFrame from "~/layouts/SiteFrame.astro";
import { ResultStreamContent } from "~/views/others/ResultStreamContent";
import { getCommonYearData } from "~/util/staticPaths";
import { locales } from "@paraglide/runtime.js";

export async function getStaticPaths() {
  const YEAR = new Date().getFullYear();
  const common = await getCommonYearData(YEAR);
  return locales.map((locale) => ({
    params: { lang: locale },
    props: { locale, ...common },
  }));
}

const { locale, yearWithCountry, years, latestYearWithCountry } = Astro.props;
const subtitle = "Wildcard結果発表配信";
const title = "Wildcard結果発表配信 - GBBinfo";
---

<SiteFrame
  locale={locale}
  title={title}
  subtitle={subtitle}
  yearWithCountry={yearWithCountry}
  years={years}
  latestYearWithCountry={latestYearWithCountry}
>
  <ResultStreamContent locale={locale} year={yearWithCountry.year} />
</SiteFrame>
