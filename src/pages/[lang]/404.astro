---
import SiteFrame from "~/layouts/SiteFrame.astro";
import { NotFoundContent } from "~/views/NotFoundContent";
import { getHeaderFooterData } from "~/util/staticPaths";
import { locales } from "@paraglide/runtime.js";

export async function getStaticPaths() {
  const headerFooter = await getHeaderFooterData();
  return locales.map((locale) => ({
    params: { lang: locale },
    props: { locale, ...headerFooter },
  }));
}

const { locale, years, latestYearWithCountry } = Astro.props;
const pageTitle = "404";
---

<SiteFrame
  locale={locale}
  pageTitle={pageTitle}
  heroSubtitle="Page not found"
  years={years}
  latestYearWithCountry={latestYearWithCountry}
>
  <NotFoundContent locale={locale} year={latestYearWithCountry.year} />
</SiteFrame>
