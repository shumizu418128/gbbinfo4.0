---
import SiteFrame from "~/layouts/SiteFrame.astro";
import { V1ReservationContent } from "~/views/travel/V1ReservationContent";
import { getHeaderFooterData } from "~/util/staticPaths";
import { locales } from "@paraglide/runtime.js";

export async function getStaticPaths() {
  const headerFooter = await getHeaderFooterData();
  return locales.map((locale) => ({
    params: { lang: locale },
    props: { locale, ...headerFooter },
  }));
}

const { locale, years, latestYearWithCountry } = Astro.props;
const heroSubtitle = "ポーランドへ行こう\n1. 予約編";
const pageTitle = "1. 予約編 | ポーランドへ行こう";
---

<SiteFrame
  locale={locale}
  pageTitle={pageTitle}
  heroSubtitle={heroSubtitle}
  heroHeading="GBBINFO"
  years={years}
  latestYearWithCountry={latestYearWithCountry}
>
  <V1ReservationContent locale={locale} />
</SiteFrame>
