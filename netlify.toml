[build]
command = "npm run build"
publish = "build/client"

[dev]
command = "npm run dev"

[context.production]
command = "echo 'Production deploy is disabled' && exit 0"

[context.deploy-preview]
command = "npm run build"
